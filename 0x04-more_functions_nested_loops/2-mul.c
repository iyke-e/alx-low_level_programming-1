#include "main.h"
/**
 * mul - the entry point
 *
 * Description: multiplication of two integers
 * @a: variable to check
 * @b: variable to check
 * Return: always 0
 */
int mul(int a, int b)
{
	int mult = a * b;

	return (mult);
}
